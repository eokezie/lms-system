# LMS Backend

## Run Project

- Build image: `docker-compose build`
- Create containers and start: `docker-compose up -d`
- View Logs for all containers: `docker-compose logs`
- view Logs for a single container: `docker-compose logs <container-name>`
- Stop containers: `docker-compose down`
